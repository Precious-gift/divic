/**
 * @module Auth Guard
 * @description Index file for importing the JWT guard.
 */

// Import the JWT guard
export * from './jwt.guard';

/**
 * Exports the JWT guard for handling authentication using JWT tokens.
 * @see {@link JwtGuard}
 */
