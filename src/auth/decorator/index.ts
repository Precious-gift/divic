/**
 * This file exports the `GetUser` decorator from the './get-user.decorator' module.
 * The `GetUser` decorator is a custom decorator that retrieves the user object from the request object.
 * It is exported as a named export from this module.
 */

// Export the GetUser decorator from the './get-user.decorator' module
export * from './get-user.decorator';
